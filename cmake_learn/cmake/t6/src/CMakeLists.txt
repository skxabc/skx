
FIND_PACKAGE(HELLO)
ADD_EXECUTABLE(main main.c)
IF(HELLO_FOUND)
    INCLUDE_DIRECTORIES(${HELLO_INCLUDE_DIR})
    MESSAGE(STATUS "hello include path:" ${HELLO_INCLUDE_DIR})
    TARGET_LINK_LIBRARIES(main ${HELLO_LIBRARY})
ELSE(HELLO_FOUND)
    message(STATUS "hello not found")
ENDIF(HELLO_FOUND)